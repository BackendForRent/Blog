# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    title:
        label: Title
    blogs_count:
        label: Blogs count
        type: number
        relation: blogs
        useRelationCount: true
    created_at:
        label: Created at
    is_published:
        label: Is published
        type: switch
